{
  "version": "0.0.0",
  "private": true,
  "name": "chatRoomApp",
  "devDependencies": {
  },
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "http-server -a 0.0.0.0 -p 8000"
  },
  "dependencies": {
    "express": "^4.13.3",
    "socket.io": "^1.3.6",
    "bower": "^1.3.1"
  }
}
